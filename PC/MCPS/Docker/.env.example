### MCPS Docker environment example
### Customize and rename to `.env` in repo root or add to your global env vars

# Example mappings — adjust ports to match your docker host mappings
TAVILY_URL=http://localhost:3000
CONTEXT7_URL=http://localhost:8080
MEMORY_URL=http://localhost:4000
GIT_MCP_URL=http://localhost:5000

# If containers are only available on Docker network (not bound to host),
# use docker hostnames (example, when agents run in Docker or WSL):
# TAVILY_URL=http://mcp_tavily:3000
# MEMORY_URL=http://mcp_server_memory:4000
