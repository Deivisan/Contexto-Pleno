# 🧭 Status do repositório (2025-12-16)

## 🎯 Decisão atual

- **Docker em pausa no Windows** por incompatibilidades/instabilidade.
- **MCPs serão centralizados em npm/npx** (portável para PC e Android).
- O material de Docker permanece **preservado como histórico** em `PC/MCPS/`.

## 🐳 Estado observado do Docker (PC)

Coleta feita em 2025-12-16 (Windows).

### docker version
- Client/Server: **29.1.2** (Docker Desktop 4.54.0)

### Containers
Foram encontrados vários containers de MCP em estado **Restarting** e um **unhealthy** (ex.: `DevSan-MCP-context7`).

### Imagens e uso de disco
- Images: **16** (10 active)
- Espaço: **~4.895GB** (reclaimable ~3.444GB)

> Importante: nenhum comando destrutivo foi executado (remoção de imagens/volumes/containers). Isso só deve acontecer com confirmação explícita.

## 📦 MCPs via npm/npx (padrão)

- Pasta universal: `mcp/`
- Android/Termux: `Android/android/configs/` + `Android/android/mcp/`

## 🔐 Segurança / segredos

Foi detectado arquivo de config com chaves/tokens versionados no passado.

Ajustes aplicados:
- Valores sensíveis foram substituídos por **placeholders** (ex.: `${TAVILY_API_KEY}`)
- Criado template: `Android/android/configs/mcp-android.example.json`
- `.gitignore` atualizado para ignorar `*.local.json` e `.env`

## ✅ Próximos passos sugeridos

1. Criar configs locais (ex.: `Android/android/configs/mcp-android.local.json` e `mcp/mcp-windows.local.json`).
2. Atualizar documentação em `PC/` marcando Docker como legado e apontando para `mcp/`.
3. (Opcional) Quando você confirmar: **limpeza do Docker** (containers/imagens/volumes) e orientação de desinstalação segura.
